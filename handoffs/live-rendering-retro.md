# Folio 实时渲染交付失误复盘

2026-09-07。用户要求：解释错误、记住正确路径并落地。依据为本会话用户原话、截图、Git 提交和真实工具输出。

## 根因与责任

我把“已有局部实现是否可用”当成了“用户要的完整使用路径是否实现”，连续验错了窗口。不是用户没有说清楚，也不是缺少渲染组件。

| 错误 | 本会话证据 | 应有动作 |
| --- | --- | --- |
| 用近似功能替代诉求 | `9b5569e` 修独立预览刷新；主窗口仍显示管道表格和引用源码，用户截图直接证伪 | 在截图所在主编辑区复现并修复，不能以独立预览代验 |
| 被旧的轻量实现牵着走 | 已确认实时渲染仍没完成，却继续要求用户选择内存与渲染的取舍 | 完整功能作为目标，性能作为优化约束；已明确的方向直接执行 |
| 验证断层 | 原生测试、JS 组件测试、文件打开测试分别通过，但没有验证生产 EditorSurface 选了哪个实现 | 挂载真实主编辑入口，再验证渲染、编辑与保存；组件检查不冒充入口验收 |
| 解释替代执行 | 用户发截图后，我最终回复只说明“接下来应修主编辑区”；用户再次催“继续啊” | 纠正理解后持续完成已授权工作，不以道歉、解释、计划结束 |
| 图标证据不足 | 源码仓与安装包 ICNS 哈希一致；Dock/Finder 实际显示未成功核验 | 区分资源一致和显示正确；未验不能说图标问题已彻底解决 |
| 测试污染用户会话 | 上轮创建的未命名“实时验收”草稿重启后触发恢复提示 | 默认用隔离状态，进入用户会话的测试草稿当轮清理 |

## 正确路径与已经取得的证据

1. 用用户实际主窗口、实际文档复现。明确“打开即渲染 → 点击块编辑 → 离开后重新排版 → 保存 → 重开”的验收链。
2. 复用本项目已有完整渲染组件，接到生产 `EditorSurface`，保留 Swift 文件与恢复管理；不要另造相邻窗口交差。
3. `89473d1` 完成主编辑接线、引用定义隐藏、保存前捕获正文，并同步 README/catalog 的架构口径。原生版历史内存值不沿用。
4. 上轮安装版 CUA 已在主窗口点击表格、输入“已实时更新”、点击下一段；AX 与截图显示网格单元格“修改前已实时更新”。没有打开独立预览窗口。随后清空关闭测试草稿并回到用户原文档。
5. 本轮增加 `Tests/MainEditorTests.swift`：真实挂载生产 `EditorSurface`，使用隔离文件和打包资源，验证表格、引用、原文保真、编辑后重渲染、Swift store 与磁盘保存。此项是入口集成测试；安装版 UI 仍由 CUA 实测，不能互相替代。

## 防复发落点

| 落点 | 内容 |
| --- | --- |
| `Tests/MainEditorTests.swift` | 覆盖原来漏验的生产主编辑表面，不单独创建正确的预览组件 |
| `scripts/test.sh` | 正常测试包含主编辑集成测试；`--main-editor <Resources>` 可单独验证 |
| `build.sh` | 签名后、安装前执行主编辑集成测试；失败停止，不跳过 |
| 总部 `projects/-Users-tianli/memory/feedback-app-quality.md` | 合并 Folio 案例、窗口范围、图标证据与测试草稿清理 |
| 总部 `projects/-Users-tianli/memory/feedback-honor-exact-ask-not-approximation.md` | 明确诉求不能用近似实现替代；纠正后直接执行 |
| 总部 `skills/appmac/WORKFLOW.md` | 把“发生位置”加入既有产品验收流程，分清组件与入口证据 |

总部原版根：`/Users/tianli/Dev/tools/cc-home`。沿用已有记忆和领域流程，没有新增全局 hook 或第二套同义规则。

反向验证：仅在 `build/main-editor-negative/BackendClient.swift` 的隔离副本把 `EditorSurface` 的 `live: true` 改为 `false`，保留完整预览和其他实现；测试退出非零并输出 `FAIL production main EditorSurface mounts its full renderer`。生产源码未回退。证据：`build/main-editor-negative/result.txt`。

正常验证：8 项入口检查通过，结果位于 `build/main-editor-test-results.txt`；测试状态在 `build/main-editor-tests-data/`。剩余边界：Dock/Finder 图标的最终显示仍不在此次实时渲染验收证据内，不能把它写成已完成。
